{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["datasetId", "target", "model"],
  "properties": {
    "datasetId": {"type": "string"},
    "target": {"type": "string"},
    "model": {"type": "string", "enum": ["logistic_regression"]}
  }
}
